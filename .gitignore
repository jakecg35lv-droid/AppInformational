# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
*.swp
*.swo

# FUSE/OneDrive artifacts left behind by in-place file edits.
# These are byte-identical copies of real pages and must never be
# committed — they show up as duplicate content to search engines.
.fuse_hidden*
**/.fuse_hidden*
